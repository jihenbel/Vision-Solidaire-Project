{% extends 'base.html.twig' %}

{% block title %}Créer une formation{% endblock %}

{% block body %}
<h3 class="card-title">Créer une formation</h3>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-dark" style="margin: 15px 40px" type="submit" value="Créer"/>
        {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('formation_index') }}" class="btn btn-dark">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
